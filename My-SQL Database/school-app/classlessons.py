class classlessons:
    def __init__(self, classid, lessonsid, teacherid):
        if id is None:
            self.id=0
        else:
            self.id =id
        self.classid = classid
        self.lessonsid = lessonsid
        self.teacherid = teacherid